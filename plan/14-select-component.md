# 下拉组件与审阅入口修复

- select.js 为所有现有原生 select 添加组件类；select.css 统一触发器、选项弹层、选中标记、焦点、禁用状态与深浅主题变量。
- 使用 appearance: base-select 渐进增强。当前应用浏览器支持；不支持的浏览器保留原生弹层。保留 value、表单提交、动态 options、change 事件和原生选择语义。
- 紧凑样式用于故事路线与对话模式，标准样式用于设定和全 AI 参数。
- 浏览器检查：对话模式弹层及鼠标选择成功；章节数选 5 后预算更新为 6 次调用；390px 窄屏弹层无越界。方向键自动化没有获得可靠的变化证据，不计作已通过的键盘回归。
- 修复连续写作历史中已定稿章节仍指向搁置原稿的问题。服务返回显式修改链上的已定稿稿件与其场景 ID，界面直接打开该正文；如正文已被后续修订替换，不按标题猜测目标。
- 31 项后端测试通过，包含重新打开已定稿修改稿的断言；前端格式与语法检查通过。
